[jobs.webserver]
command = ["cargo", "run", "--bin", "hcwnd_server"]
need_stdout = true
background = false
on_change_strategy = "kill_then_restart"
kill = ["kill", "-s", "INT"]
watch = [
  "../../assets",
  "../../package.json",
  "../../bun.lock",
]
scroll_anchor = "auto"

[jobs.webserver.env]
HC_VALKEY_URL = "redis://localhost:6379"
HC_DATABASE_URL = "postgresql://postgres:example@localhost:5432/timetable"
HC_SERVER_PORT = "8090"
RUST_LOG = "debug"
RUST_LOG_STYLE = "always"
HC_IMAGE_PROXY__SIGNING_SALT = "hello"
HC_IMAGE_PROXY__SIGNING_KEY = "secret"
